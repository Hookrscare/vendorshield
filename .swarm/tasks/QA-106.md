# Task Packet: QA-106 — Hardened InsForge Persistence Cutover

```yaml
id: QA-106
title: Harden authenticated VendorShield persistence and role enforcement
owner: Antigravity
status: READY_FOR_EXTERNAL_AGENT
objective: Replace authenticated company/vendor process-memory operations with tenant-scoped InsForge repositories while preserving the anonymous fictional demo and closing viewer/update/audit gaps before editing is enabled.
why_this_agent: This is a cross-cutting database policy, authorization, API, mapping, and negative-test migration that benefits from large repository context and exhaustive security analysis.
repository: /Users/castro/Documents/SaaS
base_commit: 6c977a0
owned_files:
  - migrations/*_harden-vendor-persistence.sql
  - src/lib/insforge/context.ts
  - src/lib/insforge/repository.ts
  - src/lib/insforge/database.types.ts
  - src/app/api/vendors/route.ts
  - src/app/api/vendors/[id]/route.ts
  - src/app/api/company/route.ts
  - src/app/api/public/[slug]/route.ts
  - src/app/dashboard/page.tsx
  - focused regression tests colocated with the files above
  - .swarm/results/QA-106.md
read_only_files:
  - AGENTS.md
  - .swarm/PROTOCOL.md
  - migrations/20260830092946_vendorshield-core.sql
  - migrations/20260830093311_onboarding-and-entitlement-indexes.sql
  - src/lib/db.ts
  - src/lib/types.ts
  - src/lib/auth/**
  - src/app/login/**
  - src/app/onboarding/**
forbidden_actions:
  - expose or request secret values
  - mutate Stripe, Vercel, DNS, email, InsForge cloud, or production state
  - apply migrations to any backend
  - create users or seed customer records
  - enable live payments or paid services
  - merge, rebase shared branches, or push main
inputs:
  - existing organization-scoped InsForge schema and RLS migrations
  - existing passwordless auth and atomic create_organization onboarding flow
  - anonymous ACME data must remain explicitly fictional and read-only
acceptance_criteria:
  - viewer role cannot insert, update, or delete vendors at both RLS and route layers
  - tenant, creator, identity, and server-maintained timestamp fields cannot be changed by clients
  - vendor mutations create exactly one immutable audit event in the same transaction
  - authenticated reads and writes derive organization identity from the verified session, never request bodies
  - every list query names columns, applies deterministic ordering, and is bounded
  - anonymous demo GET behavior remains intact and anonymous mutations remain 403
  - cross-tenant or invisible vendor IDs return 404 without enumeration clues
  - company updates are admin-only and atomic
  - public register projection returns only public-safe company fields and is_public vendors
  - unknown non-demo public slugs return 404 instead of ACME data
  - dashboard edits enable only for owner, admin, or member after durable routes are verified
  - raw SQL, InsForge, auth, or credential errors never reach clients
required_checks:
  - migration review against InsForge access-control and integrity guidance
  - focused mapper/repository/route tests
  - viewer and cross-tenant negative authorization tests
  - npm run test:run
  - npm run build
  - npm run lint with no new errors
  - npm audit --omit=dev
  - secret scan of changed files
expected_artifacts:
  - one unapplied SQL migration
  - tenant context and repository implementation
  - converted routes and dashboard access metadata
  - focused regression tests
  - .swarm/results/QA-106.md with exact evidence and rollback notes
known_risks:
  - incorrect RLS helper ownership or search_path could bypass or recurse
  - partial company updates without an RPC could leave inconsistent state
  - audit triggers can duplicate events if route code also writes audit rows
  - public projections can leak notes, contacts, or private vendors
dependencies:
  - QA-103
  - commit 6c977a0 auth/onboarding milestone
token_class: heavy
```

## Instructions

Work in an isolated branch or worktree based on `6c977a0`. Edit only `owned_files`.
Do not apply the migration or call production services. Return a result packet using
`.swarm/RESULT_TEMPLATE.md`; Root Codex owns independent review, backend-branch
application, integration, deployment, and release decisions.
